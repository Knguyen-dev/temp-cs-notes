

#### Setup venv
```Bash
# Create venv
python -m venv venv

# Activate venv
venv/bin/activate

# This also works for activation
venv\bin\Activate.ps1

python -m pip install --upgrade pip

# Add gitignore
echo "*" > .venv/.gitignore
```

